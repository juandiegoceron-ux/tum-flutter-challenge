name: tum_flutter_challenge
description: A new Flutter project for the T.U.M. challenge.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.4
  go_router: ^14.1.4
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  intl: ^0.19.0 # Para formatear moneda

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
